<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Every 15 minutes: the tightest milestone is an hour out, so the
             run has to be well inside that to land near the mark. -->
        <record id="ir_cron_crm_meeting_reminders" model="ir.cron">
            <field name="name">CRM: Send Meeting Reminders</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_meeting_reminders()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
